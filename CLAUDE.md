# CLAUDE.md — hướng dẫn làm việc trong repo này

Mai Lowtechie là trợ lý AI chief-of-staff cá nhân của Mai. **Nguồn sự thật sản phẩm là `docs/PRD.md` (v3.2, 23/9/2026)** cùng hai prototype trong `docs/prototypes/`. Khi PRD và code lệch nhau, ưu tiên PRD hoặc hỏi Mai.

**Phân loại ảnh TRƯỚC, trích SAU (§5.1.1 v3.1 — lỗi thật 23/9 "gửi banner mà app bảo không đọc được dòng việc nào"):** `kind` của `/api/parse-image` giờ là checklist · banner · **chat** (screenshot chat/email → items kèm người + hạn) · **document** (tài liệu → việc + mốc) · **danhthiep** (→ `contact` {name, org, phone, email} → thẻ `ContactCard` ở Giao việc: chọn dự án + loại, `findClientByName` chống trùng, phone/email vào `Client.contact`) · **khac**. **KHÔNG BAO GIỜ trả lời cụt:** kind `khac` (kể cả khi đọc lõm bõm được vài dòng) hoặc items rỗng → thẻ `unknown` hiện những-gì-đọc-được + `readNote` (lý do mờ/chữ nhỏ/lóa + đề nghị gửi ảnh gốc) + hỏi đúng một câu "Ảnh này là sự kiện, danh sách việc, hay ghi chú?" + nút Tạo sự kiện thủ công (mở BannerCard trống) / Gõ việc thủ công — đừng đưa lại câu "Mình không đọc được dòng việc nào". `readNote` cũng kèm vào savedLines khi trích được. **Dự án của banner:** `event.projectHint` (AI đoán theo logo/tổ chức, phải qua `sanitizeTaxonomy`) thắng `classify(title+organizer)`. **Lịch đích hiện SẴN theo dự án** trên BannerCard (`bookAcct` init từ `settings.projectCalendar`, đổi dự án thì đổi theo trừ khi Mai đã tự chọn — cờ `acctTouched`). **Book thẳng banner (v3.1, Mai opt-in TỪNG dự án):** `settings.autoBookBanner` (store v13, `setAutoBookBanner`, toggle ⚡ ở `/ket-noi`); điều kiện trong `canAutoBookBanner` (core/banner, có test): giờ chắc chắn (không đang hỏi khung giờ) + có địa điểm + không trùng — thiếu gì là rơi về thẻ xem trước; book thẳng LUÔN báo lại + nút "Hoàn tác book", việc "Đăng ký / mua vé" vẫn qua Hộp duyệt.

**Ảnh banner sự kiện → lịch (§5.1.1 v3.0):** `/api/parse-image` giờ PHÂN LOẠI ảnh (`kind: checklist/banner/khac`) — banner/poster/thiệp mời → trả `event` (BannerEvent) thay vì items; AI trích thô + suy NĂM theo mốc thời gian thực ("không ghi năm → lần gần nhất TỪ HÔM NAY trở đi"), còn quyết định là của CODE: `src/core/banner.ts` (CÓ TEST) — `resolveBannerTiming` (đã qua → báo "đã diễn ra" không tạo; thiếu giờ/nhiều khung giờ → hỏi ĐÚNG MỘT CÂU với các ứng viên tương lai; sự kiện đang diễn ra vẫn tạo được; thiếu endAt +2h), `findDuplicateEvent` (cùng tên fold + cùng ngày → nút "Cập nhật sự kiện đã có" qua `updateEvent`, không tạo trùng), `bannerNote`. **QR:** AI KHÔNG đọc được QR — client giải bằng jsQR (`decodeQr` trong `src/lib/image.ts`, dependency `jsqr`) rồi gửi `qrUrls` kèm request; server ưu tiên link chữ, rơi về QR. Thẻ `BannerCard` (giao-viec): sửa tên, chọn giờ (nút `picked` — chọn xong THÔI hỏi, đừng bỏ cờ này), dự án đoán bằng `classify(title+organizer)`, Mở Maps, tick Book + lịch đích (§5.3.4), việc **"Đăng ký / mua vé"** vào HỘP DUYỆT với hạn = hạn đăng ký (hạn cứng); ảnh banner gốc → IndexedDB (`putFile`, id `banner-*`), sự kiện mang `linkUrl`/`notes`/`bannerImage` — màn Lịch vẽ nút 🔗 và 🖼 (`BannerFileLink` — object URL nạp SẴN qua useEffect, bài học popup).

**KHÔNG còn giờ/tuần (v2.8 — lỗi thật "0h / mục tiêu 12h" gây nhiễu):** `Project` không có `weight`/`targetHoursPerWeek` nữa — đừng thêm lại. Ưu tiên giữa dự án = **thứ tự Mai sắp** (`projectOrderBoost` trong `src/core/priority.ts`, đầu danh sách +2 giảm dần, hạn sát luôn thắng). `weekStats` (`src/core/stats.ts`) ĐẾM việc: doneCount/lateCount/openCount + `progressed`; `mostStarved` = dự án đứng trên cùng còn việc mở mà cả tuần chưa xong việc nào; Review tuần hiện "X xong · Y trễ", không thanh giờ. `projectCounts` + `next7DaysRange` (ngày ĐỊA PHƯƠNG hôm nay → hết ngày +7) dùng chung cho ô dự án và brief.

**Màn Dự án & chi tiết dự án (§5.3.0 v2.9):** `/du-an` CHỈ là lưới ô — mỗi dự án một ô chạm được (kể cả Cá nhân/Học tập/Admin — lỗi cũ gộp một dòng không chạm được), hiện mở/quá hạn/7 ngày, KHÔNG kèm danh sách "Việc đang mở" (trùng Hôm nay). **Dự án không còn dòng mô tả/goal (v2.9)** — `Project` chỉ tên + màu (+status), đừng thêm lại field goal. Chạm ô → `/du-an/[id]`: **hai dropdown MỘT DÒNG** "Nhóm: Category/Khách/Hạn" · "Lọc: Tất cả/Có hạn/Không hạn/⭐ Cao/Cộng sự/Quá hạn" (nhãn NGẮN kẻo cắt chữ; thay tab + hàng chip cũ; app NHỚ lựa chọn — `settings.projectGroupBy/projectFilter`, store v11, `setProjectView`); nhóm category có "Đã xong (N)"; nhóm Khách có **"Chưa gắn khách (N)"**: chạm tiêu đề → gắn cả nhóm, ô 🤝 trên từng dòng (`TaskRow` prop `trailing`, stopPropagation — chạm dòng vẫn mở chi tiết) gắn một việc một chạm qua `setTaskClient`, kèm "Tạo khách mới". **Ô "+ Thêm việc" đầy đủ trường (v2.9):** Tên · Ghi chú · Category · Khách · Deadline (DueEditor) · ⭐; nút 🎤 trong ô — nói một câu → `parseToTask` (POST `/api/parse` kèm taxonomy, rơi về `parseCommand`) tự điền; **thiếu category/khách/hạn → hỏi ĐÚNG MỘT CÂU một lúc** (2–3 lựa chọn đầu theo thứ tự Mai + "Bỏ qua", vẫn lưu được với ô trống); lưu xong ô trống lại nhưng GIỮ category để nhập liên tiếp; nguồn `app-voice` khi tạo bằng nói. **Deadline 7 ngày tới ở Hôm nay (§5.3.3):** tiêu đề có số đếm "(N)", nhóm theo ngày, KHÔNG `slice` âm thầm — quá 8 dòng thì phần sau vào `<details>` "Xem tất cả (N)"; test 20+ việc cùng hạn ngày mai trong `core.test.ts` — đừng thêm lại giới hạn cứng.

**Nhiều tài khoản Google/Lark (§5.3.4 v3.0):** màn **`/ket-noi`** — mỗi tài khoản một dòng (chip Google/Lark + email), bật/tắt riêng **Lịch · Mail · Drive**, Kết nối lại, Ngắt từng cái; nút "＋ Tài khoản Google" (`/api/google/auth?add=1` → `prompt=consent select_account`) và "＋ Tài khoản Lark" (`/api/lark/auth`). **Kiến trúc:** `src/lib/accounts.ts` — MỖI tài khoản một cookie httpOnly mã hóa AES-GCM; cookie Google CŨ `lowtechie_g` GIỮ NGUYÊN tên + khóa, đọc thành tài khoản id **`g0`** (Mai không phải nối lại — đừng đổi khóa dẫn xuất `lowtechie-cookie::GOOGLE_CLIENT_SECRET`); tài khoản thêm sau nằm ở `lta_g_<slot>`/`lta_l_<slot>` (Lark khóa riêng từ `LARK_APP_SECRET`); bật/tắt nằm TRONG cookie (`parts`), sổ đọc/ghi qua `/api/accounts` (GET/POST/DELETE), callback trùng email thì ghi đè đúng slot giữ parts. **Lark (`src/lib/larkServer.ts`):** OAuth v2 `authen/v2/oauth/token` scope **`offline_access calendar:calendar`** — token NGƯỜI DÙNG chỉ mang scope đã XIN trong authorize URL, xin thiếu là gọi lịch dính 99991xxx "không quyền" dù admin đã duyệt app (lỗi thật 23/9; đổi scope thì Mai phải Kết nối lại, refresh token cũ không tự thêm quyền; ĐỪNG xin mail — best-effort, xin mà app chưa khai quyền là vỡ đăng nhập 20027), env `LARK_APP_ID`/`LARK_APP_SECRET`, redirect `/api/lark/callback` phải đăng ký ở Security Settings của app Lark; **Lark XOAY VÒNG refresh token — route nào lấy access token PHẢI ghi lại cookie** (mẫu `rotated[]` trong calendar/flights/accounts-probe); Calendar v4 GHI vào lịch chính, ĐỌC mọi lịch con (v3.2); **Lark Mail best-effort** (PRD §9: gói tổ chức có thể chưa bật Mail API) — lỗi thành ghi chú `mailboxNotes`, không vỡ cả lần quét. **Lịch:** `/api/calendar/events` GET gộp MỌI tài khoản bật Lịch + khử trùng mời chéo theo iCalUID (`dedupeRemoteEvents` trong `src/core/events.ts`, có test) + tag `account/accountEmail`; POST nhận `accountId` (lịch đích), DELETE `?account=`; block lưu `calAccount` để xóa đúng nơi (block cũ không có → g0). **Lịch đích theo dự án:** `settings.projectCalendar` (store v12, `setProjectCalendar`, UI ở `/ket-noi`); thẻ xem trước Giao việc có select "Lịch đích" khi >1 tài khoản; chuỗi bay lấy theo dự án của sự kiện chính. **Quét vé đa hộp thư:** `/api/gmail/flights` chạy từng hộp bật Mail (Gmail đầy đủ; Lark text-only), mỗi hộp một lời gọi Claude, chặng gộp chung MỘT lần `classifyAndGroup` (vé forward qua 2 hộp không thành 2 chuyến — có test), candidate/history mang `mailbox`, refs vé mang `account` cho `/api/gmail/attachment?account=`. `useAccounts()` trong `src/lib/useGoogle.ts` (toggle optimistic); `/api/google/status` giữ nguyên hình dạng cho màn cũ (connected/gmail = BẤT KỲ tài khoản Google nào).

**Lark Calendar đọc VỀ app (PRD v3.2 — lỗi thật 23/9 "nối Lark rồi mà sự kiện không hiện"):** Lark trả **HTTP 200 kèm `code != 0`** khi lỗi quyền/token — mọi lời gọi calendar đi qua `larkJson` (`src/lib/larkServer.ts`) ném `lark-<code>`, ĐỪNG bao giờ quay lại check `res.ok` trần (đó chính là nguyên nhân lịch "trống" im lặng). Đọc lịch: `larkListCalendars` (phân trang page_token) → `larkEventsAllCalendars` đọc **MỌI lịch con** (bỏ type `resource`, cap 10, khử trùng event_id; MỘT lịch lỗi không làm rỗng cả tài khoản, mọi lịch cùng lỗi mới ném; 0 lịch → `lark-nocal`); `larkListEvents` cũng phân trang; tìm kiếm `larkSearchEvents(at, q)` quét mọi lịch. Lỗi → `larkErrorAction` + `accountErrorAction` (accounts.ts) thành thông điệp **có việc để làm** ("admin duyệt quyền Calendar", "Kết nối lại") — CÓ TEST `src/lib/__tests__/larkServer.test.ts`, đừng xóa. `/api/calendar/events` GET trả `errors[]` kèm `action`; màn Lịch vẽ hộp cảnh báo + nút "Mở Kết nối" (không để lịch trống im lặng) và **dấu tài khoản** trên dòng sự kiện khi ≥2 email (nhãn = phần trước @, trùng nhau thì domain); `useGoogleEvents` trả thêm `errors` + tự làm mới 15 phút khi màn còn mở. **Trạng thái đồng bộ (màn Kết nối):** `/api/accounts?probe=1&fromMs&toMs` đọc thử từng tài khoản bật Lịch (đếm lịch con + sự kiện, nhớ mẫu rotated Lark); `settings.syncStatus` (store v14, `setSyncStatus`) giữ lần gần nhất; màn Kết nối TỰ probe khi có tài khoản chưa đồng bộ lần nào (để trong 1 phút sau khi nối Mai thấy ngay số sự kiện), nút "Đồng bộ ngay", 0 sự kiện → cảnh báo vàng, lỗi → dòng ⚠ kèm hành động.

**Danh bạ ↔ voice/ảnh (§5.3.2 v2.8):** tên khách + tên gọi khác được gửi làm từ vựng ưu tiên cho Whisper (`vocab` trong body `/api/stt`, `clientVocab()` ở `src/lib/speech.ts`, cap 40 tên). `matchClientDetail` trả kèm cụm chữ khớp → dòng «nhận từ "đô thị"» trên thẻ duyệt/Giao việc. **Học cách gọi:** Mai gõ tên khớp khách đã có (kể cả không dấu) ở ô khách → `withLearnedAlias` (cap 12, so norm phân biệt dấu để "do thi" đáng học) + `updateClient` — cả đường Lưu lẫn onCreate ở hop-duyet/giao-viec. Quản lý dự án: ô "+ khách hàng" (chữ NGẮN — placeholder dài bị cắt là lỗi 22/9), chọn loại ngay khi thêm, form không đóng sau mỗi lần Thêm; dự án KHÔNG còn ô h/tuần lẫn ô mô tả (v2.9 — chỉ tên và màu).

**Chi tiết việc & đóng việc (PRD 5.2.2 v2.6 — sửa 3 lỗi thật 22/9):** CHẠM vào dòng việc là mở `TaskDetail` (`src/components/`), KHÔNG BAO GIỜ đóng việc — đóng CHỈ bằng ô tick đầu dòng hoặc nút "Xong ✓" trong chi tiết (`completeTask(id, via)` ghi `completedVia`); tick xong hiện "Hoàn tác" 6 giây ngay trên dòng (trong `TaskRow` — đừng đổi lại thành `<label>` bọc cả dòng, đó chính là lỗi cũ). Đóng qua chat ("xong việc X rồi" → kind `complete` trong parser) LUÔN qua thẻ xác nhận hiện đúng tên việc khớp ở Giao việc. Việc đã xong vào mục **"Đã xong"** thu gọn cuối danh sách (Hôm nay + Dự án, lọc tuần/tháng/tất cả), nút **Mở lại** (`reopenTask`; hạn đã qua → gợi ý đặt hạn mới trong chi tiết). **Hiển thị hạn:** `fmtDue` (`src/lib/format.ts`, có test) — 0:00 và 9:00 là quy ước "chỉ ngày", KHÔNG hiện giờ (lỗi "hạn 30/10 0:00"); chỉ hiện giờ Mai thật sự đặt. **Ưu tiên hôm nay:** việc còn hạn >14 ngày không lên `brief.top` trừ khi `priority: "high"` (nút ⭐ trong chi tiết) hoặc `blocksOthers` (test trong `v26.test.ts`).

**Ghi chú từng việc (PRD 3d v2.6):** `Task.notes` = nhật ký `TaskNote[]` (body + at, sửa/xóa từng dòng) — **TÁCH RIÊNG với trích dẫn Nguồn** (`source.quote` giữ nguyên chỗ cũ); ô "Ghi chú (tùy chọn)" trên thẻ duyệt + thẻ Giao việc, nhật ký đầy đủ trong `TaskDetail`; dòng 📝 N trên `TaskRow`. Lệnh chat "ghi chú cho việc X: …" → kind `note` (parser + schema Claude), tìm việc theo tên rồi `addTaskNote` — không tạo việc mới.

**Địa điểm đã lưu (places §8):** `Place` giờ có `address` + `city` (bkk/hcmc/tokyo) + `isHome` — quản lý ở mục "📍 Địa điểm của Mai" màn Lịch, mỗi nơi có link "Mở Maps". Chuỗi ngày bay lấy MẶC ĐỊNH điểm đi/đến theo THÀNH PHỐ của đầu chặng: nơi 🏠 cùng city thắng, bkk rơi về `settings.homeAddress`, và `PlaceSelect` (`src/components/`) cho chọn nhanh ở cả FullChain lẫn ChainForm. `iataCity` (core/flights) đổi mã sân bay → city. **Bài học popup:** nút "Mở" file vé phải là THẺ `<a href={blobUrl}>` nạp sẵn qua useEffect — `window.open` sau `await` bị Safari/PWA chặn (lỗi thật "không mở được vé"); kèm nút Tải (`download`).

**Nhắc đặt lịch trước (PRD §5.4.2 v2.6):** cùng danh bạ `places` (needsBooking, bookingLeadDays, bookingMethod, bookingContact). Sự kiện có location/tên khớp nơi needsBooking → `bookingStatus: "pending"` + `placeId`, đồng thời việc **"Đặt lịch {nơi} cho {giờ}"** tự vào HỘP DUYỆT với hạn = ngày hẹn − leadDays (hạn cứng). UI Lịch: chip "🔖 Chưa đặt" (bấm → xác nhận → booked), nút Gọi/Mở đặt chỗ theo bookingContact, cảnh báo "<24h chưa đặt — có thể không thành", icon 🔖 trên ô tháng. Lowtechie KHÔNG tự gửi tin đặt chỗ — Mai tự gọi/gửi rồi bấm Đã đặt.

**Màn Lịch (§5.4.0 v2.3):** 4 chế độ Ngày · Tuần · Tháng · Danh sách, app NHỚ chế độ lần trước (`settings.calendarView`, store v9). Lưới tháng (`MonthGrid` trong `src/app/lich/page.tsx`): chấm màu theo dự án (tối đa 3 + "+N"), biểu tượng ✈️ (ngày bay) · ❗ (hạn cứng) · 📄 (hẹn định kỳ); chạm ngày → danh sách bên dưới; ◀ ▶ + "Hôm nay" + chọn tháng/năm, **không giới hạn quá khứ/tương lai** — dữ liệu Google tải THEO THÁNG đang xem qua `useGoogleEvents(from,to)`, đừng tải trước toàn bộ. Tìm kiếm TOÀN BỘ lịch: local fold-dấu + Google qua `/api/calendar/events?q=` (không giới hạn thời gian). Lọc: dự án · loại · nguồn (app/Google).

**Hẹn định kỳ dài hạn (§5.4.0 v2.3):** `src/core/series.ts` (CÓ TEST — kẹp cuối tháng 31/1→28/2, năm nhuận): `RecurringSeries` trong store v9, chu kỳ ngày/tuần/tháng/năm bất kỳ. **"Đã làm" ghi NGÀY THẬT → lần sau = ngày thật + chu kỳ** (`completeOccurrence`), lịch sử từng lần giữ lại. Nhắc trước 30/14/7/1 ngày (`activeReminder` — hiển thị đếm ngược, chưa có push). **Việc chuẩn bị tự vào HỘP DUYỆT** ở mốc 30 ngày, mỗi kỳ đúng một lần (đánh dấu `prepCreatedFor`; effect trong màn Lịch), mẫu việc `prepTemplate` mỗi dòng một việc, sửa được. Hẹn trùng ngày bay → cảnh báo trên dòng hẹn. Mặc định Cá nhân · Giấy tờ, hạn cứng.

**Voice (PRD §5.0 v2.0 — sửa lỗi thật 22/9):** KHÔNG dựa vào Web Speech của trình duyệt. `src/lib/speech.ts` ghi âm bằng MediaRecorder rồi gửi `/api/stt` (OpenAI Whisper qua fetch thuần, env `OPENAI_API_KEY`, model `LOWTECHIE_STT_MODEL` mặc định whisper-1; thiếu key → 501 và client rơi về Web Speech nếu có). Trạng thái rõ: đang nghe / đang xử lý / lỗi kèm lý do (quyền micro, mạng); ô gõ chữ luôn là dự phòng; nhắc xin quyền micro chỉ hiện lần đầu. Cờ `stt` nằm trong `/api/google/status`.

**Chuyến đi (PRD §5.9 v2.0):** màn GỌN — chỉ nút quét, tab chuyến SẮP TỚI (nhãn theo tuyến "SGN → BKK · 2/10", tuyệt đối không hiện tab chuyến đã bay), chuỗi ngày bay, checklist. Kết quả quét (mốc "🕐 Hôm nay", ứng viên, dòng Lịch sử) nằm trong MỘT thẻ đóng được, không nằm cố định trên màn. **Chuỗi ngày bay HAI ĐẦU**: `fullFlightChain` + `validateChainBlocks` trong `src/core/timeback.ts` (test hồi quy bảng VU-131 ở `__tests__/fullchain.test.ts`): Chuẩn bị → Ra sân bay (điểm đi + phương tiện, Maps) → Check-in (max(quy định vé, 150/90), Mai override được) → Bay → Nhập cảnh (60/30) → Di chuyển sau khi đáp → "Về đến nơi". Block chỉnh/tắt được; block sai thứ tự → hiện lỗi, KHÔNG vẽ chuỗi; block khác ngày kèm ngày; cảnh báo di chuyển >3h + chuẩn bị rơi 0:00–5:00 — **cảnh báo tính theo GIỜ ĐỊA PHƯƠNG thành phố đi, không phải UTC** (v2.3, lỗi thật "7:13 sáng bị coi là nửa đêm"): `originTzOffsetMin` = offset trong ISO → `IATA_TZ_MIN` (core/flights) cho dữ liệu cũ dạng Z → offset thiết bị; test bắt buộc 7:13 & 4:30 HCMC trong `__tests__/fullchain.test.ts`. Phương tiện (v2.3): Grab/taxi · Ô tô riêng · Tàu điện · Xe bus · Xe máy — KHÔNG có "Người đón"; tàu điện/bus → transit, xe máy → TWO_WHEELER (server tự rơi về DRIVE nếu khu vực không hỗ trợ). **Giờ bay lưu NGUYÊN ISO kèm offset sân bay** (đừng `toISOString()` trần — mất múi giờ là cảnh báo đêm sai; chuyến tạo tay dùng `deviceOffsetIso`). **Xóa chuyến (6a)**: `deleteTrip` vào `tripTrash` (10 phút, Hoàn tác `undoDeleteTrip`), thẻ xác nhận liệt kê block/GCal/checklist/file vé (mặc định giữ file), chọn nhiều trong Lịch sử, ghi rõ "không hủy vé với hãng". **Tự lưu vé PDF**: quét trả `attachments` refs; khi Mai xác nhận chuyến, client tải qua `/api/gmail/attachment`, blob vào IndexedDB (`src/lib/fileStore.ts`), metadata vào `trip.attachments` (tên chuẩn `Ve_SGN-BKK_2026-10-02_OADC5J.pdf`, trùng tên → bản mới isLatest, bản cũ giữ lịch sử).

**Book lịch có xem trước (§5.4, bản đầu):** thẻ sự kiện ở Giao việc hiện cảnh báo (trùng giờ, ngày bay) + ô tick "Book lên Google Calendar" (mặc định KHÔNG book); book xong có "Hoàn tác book Google" (xóa GCal + gỡ event local). Chưa làm: recurrence, mời người (LUÔN cần xác nhận riêng khi làm), link Meet/Lark VC, sửa-qua-thẻ-xem-trước, đồng bộ hai chiều, "book thẳng báo sau" theo loại lịch, lịch đích theo dự án.

**Trích vé máy bay (PRD §5.9 quy tắc 6b — rút từ lỗi thật vé OADC5J):** trích **theo chặng, không theo vé**. AI (tool `emit_segments` trong `/api/gmail/flights`) chỉ trả THÔ mọi chặng tìm thấy trong email **và PDF đính kèm** (tối đa 3 tệp ≤1,5MB — hành trình đầy đủ, nhất là chặng về, hay chỉ nằm trong PDF), chỉ đánh dấu `cancelled`/`superseded` theo sự kiện trong email, KHÔNG tự lọc theo thời gian. Việc lọc là của CODE: `classifyAndGroup` trong `src/core/flights.ts` (test hồi quy OADC5J bắt buộc ở `__tests__/flights.test.ts` — đừng xóa) khử trùng theo **PNR + số hiệu + ngày bay** (không bao giờ chỉ PNR), so `departLocal` với epochMs + tzOffsetMin thật của thiết bị Mai (server chạy UTC, cấm `new Date()` trần làm "hôm nay"), gán đã bay/đã hủy/lịch cũ vào Lịch sử, gộp chặng sắp tới cùng PNR thành ứng viên kèm `route` ("SGN (nhà ga 2) → BKK") + `airportBufferMin` (quy định "có mặt trước X phút" trên vé). UI: dòng mốc "🕐 Hôm nay: …"; trùng PNR → nút "Cập nhật giờ" (updateTrip), không tạo bản sao; chuỗi ngày bay đề "Cất cánh {route}"; chuyến qua (giờ về ?? giờ đi +24h) chỉ còn ở mục **Lịch sử**, không bao giờ vẽ chuỗi ngày bay nữa. Chưa làm từ §5.9: đối chiếu dịch vụ dữ liệu lịch bay + realtime 24h trước, điểm xuất phát theo thành phố của chặng về, lưu vé (PDF/QR offline).

Taxonomy theo v1.6: seed 5 dự án Sorene, Circle, Cá nhân, Học tập (3 category: Tiếng Thái · Khóa học & chứng chỉ · Đọc & nghiên cứu, màu `#9BC53D`), Admin chung (màu `#8A8FB0`); Favstay & Edge đã bỏ. Dự án/category là dữ liệu Mai tự quản (Dự án → Quản lý): thêm/sửa tên/màu (không còn mô tả — v2.9), **tạm ngưng (status archived — ẩn khỏi mọi màn chính qua `activeProjects()`)**, **xóa phải chọn dự án nhận việc** (`deleteProject(id, moveTo)`). `ProjectId` là chuỗi mở; mọi id từ `classify()`/Claude phải đi qua `sanitizeTaxonomy(projects, categories, …)` trước khi lưu (dự án đã xóa rơi về Cá nhân). Client gửi taxonomy thật (kèm danh bạ khách) với `/api/parse` và `/api/parse-image` (`src/lib/taxonomy.ts`); dự án tự thêm được phân loại nhờ học-từ-sửa (feedback), không cần thêm luật cứng.

**Khách hàng / đối tác (PRD §5.3.2, v1.6): là TRƯỜNG RIÊNG, không phải category.** Danh bạ `clients` trong store (`Client`: name, type khachhang/doitac/nhacungcap, aliases, projectIds — một khách thuộc được nhiều dự án, status, lastUsedAt/useCount). `matchClient` (`src/core/clients.ts`, có test) so tên + tên gọi tắt trong nội dung để tự điền `clientId`; **tên lạ KHÔNG đoán**; id từ Claude phải qua `sanitizeClientId`. Tín hiệu phân loại: dự án của khách khớp tên được đề lên đầu lựa chọn thay thế (`clientProjectHint`). **Nhập một lần (v2.3, lỗi thật "nhập Đô Thị xong lần sau phải nhập lại"):** tên khách Mai GÕ TAY trong ô chọn rồi Lưu — dù không bấm "Tạo mới" — vẫn vào danh bạ (SearchSelect `onQueryChange` → cha giữ query; khi lưu: `findClientByName` fold dấu/hoa thường/khoảng trắng để KHÔNG tạo trùng, không khớp mới `addClient`); mỗi lần dùng gọi `touchClient` để ô chọn gợi ý theo **gần đây → hay dùng → thứ tự Mai đặt** (`orderClientsForPick`); tìm trong SearchSelect không phân biệt dấu ("do thi" ra "Đô Thị"). Danh bạ đồng bộ máy chủ = Giai đoạn 3.

**Thẻ duyệt (PRD §5.2.1 3b — sửa lỗi thật 22/9):** 3 ô chọn RIÊNG, sửa độc lập — Dự án · Category (lọc theo dự án) · Khách hàng (lọc theo dự án, không bắt buộc) + ô Deadline; mỗi ô là `SearchSelect` (`src/components/`) có tìm kiếm và dòng **"Tạo mới: …"** tạo tại chỗ, KHÔNG quay lại danh sách phẳng "Dự án · Category". Nhận cả nhóm ảnh cho đặt chung dự án/category/khách/hạn trước khi nhận. Đổi tên/xóa category, khách làm ở màn Quản lý dự án.

**Deadline từng việc (PRD §5.2.1 3c):** `DueEditor` + `src/core/due.ts` (có test). Nguồn có hạn → tự điền (`dueSource: "nguon"`, hiện dạng đầy đủ "Thứ Sáu 25/9/2026" + trích dẫn); **nguồn không có hạn → ĐỂ TRỐNG + làm nổi ô, cấm đoán** (prompt hai route AI cũng cấm). Nút nhanh Hôm nay · Ngày mai · Thứ Sáu này · Tuần sau · Cuối tháng (9:00 sáng, cùng quy ước parse.ts); loại hạn cứng/mềm; "Không có hạn" chọn rõ; Lưu & nhận khi ô trống → nhắc đúng một lần. Cảnh báo nhẹ (`dueWarnings`): hạn đã qua · rơi ngày bay · ngày lịch dày — Mai vẫn giữ được. Đổi hạn ghi vào `dueChanges` (store v7).

**Sắp xếp thứ tự (PRD §5.3.1):** thứ tự mảng trong store CHÍNH LÀ thứ tự Mai đặt, mọi màn/ô chọn render theo đó — đừng sort lại. Quản lý dự án có chế độ "Sắp xếp": `moveProject`/`moveCategory`/`moveClient` (⤒↑↓⤓) + "Hoàn tác" (`setOrders` khôi phục snapshot); mục mới luôn vào cuối. Chuyển category sang dự án khác: `moveCategoryToProject` (đổi id theo prefix mới, việc + feedback đi theo, hỏi xác nhận trước).

**Quy tắc UI số 0 (v1.6):** màn hình KHÔNG chứa câu giải thích cách hoạt động hay tham chiếu nội bộ ("PRD §…") — hướng dẫn chỉ nằm ở empty state (lần dùng đầu). Đừng thêm lại các đoạn "mẹo"/giải thích đã gỡ.

Chưa làm từ v1.6: kéo-thả cảm ứng khi sắp xếp (đang dùng nút), gộp dự án, icon/keywords, quản lý dự án·category·khách bằng chat/voice, gợi ý cấu trúc + hoàn tác structure_changes, màn chi tiết khách hàng + weekly review theo khách + gộp khách trùng tên, nhắc việc trước hạn (cần push), sửa hạn ngay trong danh sách việc, mục "Dùng gần đây" trong ô chọn, cột Khách hàng trong Google Sheets (sync Sheets chưa làm).

Chưa làm từ v2.0: **toàn bộ Lark (§5.5.1 — bot group, Lark Mail, Lark Calendar; kênh ưu tiên của Giai đoạn 2, cần tạo app trên larksuite.com + admin tổ chức duyệt quyền)**; sửa block chuỗi bay bằng chat/voice + thêm block tùy ý + khóa một block; email không có PDF → in email thành PDF; vé từ ảnh chụp; lưu file vào Drive + gắn link vào sự kiện; tự dọn chuyến sau 90 ngày; đối chiếu dịch vụ dữ liệu lịch bay + realtime.

Chưa làm từ v2.3: hỏi lịch bằng chat/voice ("tháng 12 chị có gì?", "lần gia hạn trước là ngày nào?"); vuốt trái/phải chuyển tháng (đang dùng nút ◀ ▶); lọc lịch theo khách hàng; hỏi "Có phải Đô Thị?" khi tên GẦN giống (hiện tên khớp fold thì tự dùng lại, không hỏi); file đính kèm (biên nhận) cho từng lần hẹn định kỳ; sự kiện quá khứ xem chi tiết (ghi chú/file/recap — mới có giờ + tên); danh bạ khách đồng bộ máy chủ (Giai đoạn 3).

Chưa làm từ v2.6: đính kèm ảnh/file/ghi âm vào ghi chú (mới có chữ); Lowtechie GỢI Ý ghi chú để Mai duyệt; ghi chú "chỉ mình xem" theo quyền (chưa có chia sẻ); tìm việc theo nội dung ghi chú; ghi chú vào cột Sheets (sync Sheets chưa làm); sự kiện lịch liên quan trong chi tiết việc; gợi ý bật "cần đặt trước" theo LOẠI nơi; tin nhắn đặt chỗ soạn sẵn theo ngôn ngữ (mới có nút Gọi/link); tự nhận email xác nhận đặt chỗ; nhắc lại hằng ngày khi chưa đặt (mới có hạn việc + cảnh báo 24h); lịch định kỳ cần đặt chỗ ("đặt luôn buổi sau"); "Spa thứ Năm đặt rồi" bằng chat (mới có nút Đã đặt).

Chưa làm từ v2.8: kéo việc sang category khác trong chi tiết dự án (đang đổi qua Sửa trong chi tiết việc); decision log + recap họp + file liên quan trong màn dự án; so khớp lỗi chính tả nhỏ + hiện 2 lựa chọn khi không chắc + gợi ý "Tạo khách hàng mới?" khi tên lạ trông giống khách (hiện tên lạ vẫn không đoán, đúng nguyên tắc cũ). Màn chi tiết khách hàng, quản lý khách bằng chat, "Dùng gần đây" đã nằm ở danh sách v1.6.

Chưa làm từ v2.9: trả lời câu hỏi thiếu-trường trong ô "+ Thêm việc" bằng GIỌNG NÓI (mới bấm nhanh hoặc chọn trong ô — PRD cho phép "bấm, gõ hoặc nói"); nhớ Nhóm/Lọc theo TỪNG dự án (đang nhớ chung một cặp cho mọi dự án).

Chưa làm từ banner sự kiện (§5.1.1 v3.0): QR chỉ đọc được khi jsQR giải nổi (mờ/méo thì thôi — link CHỮ trong ảnh vẫn trích bình thường); sự kiện nhiều ngày đang là MỘT sự kiện kéo dài (chưa tách chuỗi mỗi ngày một block); ảnh banner chỉ đính bản local (GCal chỉ nhận description chữ); chuỗi Chuẩn bị + Di chuyển cho sự kiện banner làm qua nút "+ Chuỗi" ở Lịch như mọi sự kiện (thẻ banner chưa tự đề xuất); đơn vị tổ chức chưa tự vào danh bạ khách/đối tác.

Chưa làm từ v3.1: test ảnh THẬT các loại (banner gốc + chụp lại màn hình, checklist tay, screenshot chat, danh thiếp) là bước Mai bấm thử trên production (CI không có key — smoke đang mock kết quả đọc ảnh); nút "Cứ tạo N việc đọc được" ngay trong thẻ không-chắc (đang chỉ có tạo sự kiện/gõ việc thủ công); danh thiếp chưa đọc nhiều liên hệ trên một ảnh.

Chưa làm từ §5.3.4/§5.5.1 (v3.0): ô bật/tắt RIÊNG từng lịch con (v3.2 Lark đã đọc tất cả lịch con nhưng chưa chọn lọc được; Google vẫn chỉ lịch chính primary); soạn/trả lời email mặc định từ đúng hộp thư đã nhận (app chưa có tính năng soạn email); email khách hàng gắn danh bạ quét đa hộp thư (mới quét vé); toggle Drive lưu được nhưng chưa có tính năng nào dùng Drive; **Lark Mail phải kiểm chứng với tổ chức thật** (PRD §9 — endpoint mail đang best-effort, lỗi hiện thành ghi chú ở thẻ quét); bot @Lowtechie trong group Lark (danh sách v2.0).

Chưa làm từ v3.2: webhook Lark báo lịch đổi + nhập sẵn 12 tháng tới cần server lưu dữ liệu (Giai đoạn 3 — hiện đọc theo THÁNG đang xem + tự làm mới 15 phút khi màn còn mở, không đồng bộ nền khi app đóng); trạng thái đồng bộ mới đếm sự kiện tháng hiện tại (chưa đếm cả năm); lỗi từng LỊCH CON riêng lẻ gộp chung một dòng theo tài khoản (một lịch hỏng giữa nhiều lịch lành chỉ thấy khi mọi lịch cùng hỏng).

## Ngôn ngữ & giọng điệu

- Toàn bộ UI, copy, commit message: **tiếng Việt**. Code (tên biến, hàm, type) tiếng Anh.
- Persona của Lowtechie (PRD §6.1): xưng **"mình"**, gọi **"Mai"**; mỗi lần hỏi đúng **một** câu; đề xuất kèm lý do, không ra lệnh; vui vẻ nhưng nói thẳng khi Mai ôm quá nhiều việc.
- Chú ý: "mai" trong câu lệnh thường là *ngày mai*, còn "Mai" là tên người dùng — parser đã xử lý, đừng phá.

## Lệnh

```bash
npm run dev      # dev server
npm test         # vitest — bắt buộc xanh trước khi commit
npm run build    # bắt buộc xanh trước khi push
```

## Kiến trúc hiện tại (Giai đoạn 1, local-first)

- **`src/core/` là logic thuần**: không import React, không gọi mạng, mọi thời điểm là chuỗi ISO. Mỗi file có unit test trong `src/core/__tests__/`. Thêm logic mới → thêm test.
- **Phân loại (PRD §5.2.1)**: bảng luật duy nhất là `RULES` trong `src/core/classify.ts` — `parse.detectProject` cũng lấy từ đây, đừng tạo bảng từ khóa thứ hai. Category mặc định ở `DEFAULT_CATEGORIES` (`src/core/projects.ts`). Học từ sửa đổi: mọi chỗ Mai đổi dự án/category phải gọi `recordFeedback(learnableTerms(title) → entries)`; `classify()` với feedback thắng luật. Ảnh nguồn của nhóm triage lưu tạm trong `triageImages` và bị xóa khi nhóm duyệt xong — đừng giữ ảnh lâu trong localStorage.
- **Store**: Zustand + persist vào `localStorage` (key `lowtechie-v1`), xem `src/lib/store.ts`. Ngày giờ lưu dạng ISO string, không lưu `Date`. Đây là chỗ sẽ thay bằng Supabase (+RLS) ở Giai đoạn 3 — giữ mọi truy cập dữ liệu đi qua store, đừng đọc localStorage trực tiếp.
- **Màn hình** trong `src/app/` là client component; mọi màn hình phải render được khi store rỗng (empty state tử tế).
- **`/api/parse`**: proxy Claude API (fetch trực tiếp, không SDK) khi có `ANTHROPIC_API_KEY`; không có key thì trả 501 và **client tự chạy `src/core/parse.ts` trên trình duyệt** — cố ý như vậy để ngày giờ tính theo múi giờ của Mai chứ không phải server (UTC). Model qua env `LOWTECHIE_MODEL`, mặc định `claude-sonnet-5`.
- **Google Calendar (PRD §5.4)**: OAuth thuần fetch trong `src/lib/googleServer.ts` — refresh token MÃ HÓA AES-GCM nằm trong cookie httpOnly (khóa dẫn xuất từ `GOOGLE_CLIENT_SECRET`; đổi secret = mọi thiết bị nối lại), server không lưu gì (đúng local-first, mỗi thiết bị tự nối). Routes: `/api/google/{auth,callback,status,disconnect}` + `/api/calendar/events` (GET/POST/DELETE). Client chỉ đụng qua `src/lib/useGoogle.ts`. Block ghi sang GCal mang `gcalId` trong store để dedupe khi fetch về và xóa được khi gỡ chuỗi. Ghi lịch LUÔN sau bước Mai bấm duyệt. Scope: `calendar.events` + `gmail.readonly` + email — cờ `gm` trong cookie cho biết đã cấp Gmail chưa (cookie nối trước khi thêm scope thì chưa, UI đưa nút nối lại). `/api/gmail/flights` quét email vé máy bay (kèm PDF đính kèm) → Claude trích chặng thô, `src/core/flights.ts` phân loại/gộp thành ứng viên, chỉ tạo trip khi Mai bấm ở màn Chuyến đi. `/api/maps/route` (Routes API, cần `GOOGLE_MAPS_API_KEY`): transit dùng `arrivalTime` (giờ đến), tách walk/transit/walk đổ vào form chuỗi; drive chỉ có `departureTime` nên là ước lượng — kết quả chỉ điền form, Mai vẫn bấm Khóa.
- Font nạp bằng `<link>` Google Fonts (giống prototype) — đừng đổi sang `next/font` (build offline sẽ vỡ).

## Design system

- Token màu ở `src/app/globals.css`, lấy đúng từ mockup: vàng mai `#FFC93C` (hành động chính), mực chàm `#1E2150` (chữ/dữ liệu), nền sương `#EEF1F8`, má hồng `#FF8FA3`, xanh lá `#2FA97C` **chỉ dành cho "đã xong"**.
- Màu dự án cố định: Sorene `#8B7BFF` · Circle `#1FA9B8` · Favstay `#FF8A5B` · Edge `#3D62E0` · Cá nhân `#FF7FA8` · Học tập `#7C9A3E` · Admin chung `#7D8AA5` (hai màu cuối không có trong mockup, đã chọn thêm).
- Chữ: Baloo 2 (tiêu đề, số, lời Lowtechie), Be Vietnam Pro (nội dung). Dark mode theo `prefers-color-scheme` + override `data-theme`.
- Linh vật bông mai: component `src/components/Blossom.tsx` (SVG 5 cánh mặt cười, lấy từ prototype). Bông mai cũng là nút giao việc giữa tab bar.

## Nguyên tắc sản phẩm phải giữ trong mọi tính năng

1. **Duyệt trước, tự động sau** — không có hành động ra ngoài (ghi lịch, gửi tin, gửi recap) nào không qua bước Mai bấm duyệt.
2. **Luôn có nguồn** — việc trích tự động phải kèm trích dẫn gốc + độ tin cậy.
3. **Hỏi lại tối đa một câu** khi thiếu thông tin.
4. Mọi tính năng đều dùng được bằng **chat hoặc voice**, màn hình chỉ để xem/duyệt nhanh (PRD §5.0).

## Việc lớn tiếp theo (theo README, PRD §10)

Kiểm chứng Lark Calendar/Mail với app thật của The Circle (Mai đã tạo app "Mai Lowtechie", chờ admin duyệt quyền + dán `LARK_APP_ID/SECRET` vào Vercel) → hoàn thiện §5.4 book lịch (recurrence, mời người — luôn xác nhận riêng, Meet link, đồng bộ hai chiều, lịch con từng tài khoản) → sync Google Sheets (cột Khách hàng) → ghi âm họp + recap → **Giai đoạn 2: bot group @Lowtechie trên Lark (đọc-toàn-bộ cho group nội bộ; cần đăng ký sự kiện tin nhắn + admin duyệt), sau đó bot 1:1 WhatsApp/Zalo** → Giai đoạn 3 (Supabase + RLS, danh bạ khách đồng bộ máy chủ).
