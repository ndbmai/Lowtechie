# Tách câu lệnh bằng Claude API (không bắt buộc — thiếu key sẽ dùng bộ phân tích luật)
ANTHROPIC_API_KEY=
# Model cho việc tách lệnh (mặc định claude-sonnet-5)
LOWTECHIE_MODEL=

# ── Giai đoạn sau (chưa dùng) ─────────────────────────────
# Google Calendar / Drive / Sheets / Gmail OAuth
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# Google Maps Routes API (chuỗi chuẩn bị + di chuyển §5.4.1)
# GOOGLE_MAPS_API_KEY=
# Supabase (Giai đoạn 3 — tài khoản cộng sự + RLS)
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
